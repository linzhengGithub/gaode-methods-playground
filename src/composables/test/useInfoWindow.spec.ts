import { describe, expect, it } from 'vitest'
import { useInfoWindow } from '../useInfoWindow'

describe.skip('infoWindow test group', () => {
  it('infoWindow is open', async () => {
  //   const { createInfoWindow, infoWindow, openInfoWindow } = useInfoWindow()

    //   createInfoWindow('content')
    //   openInfoWindow(infoWindow.value, [116.397389, 39.909466])

  //   expect(infoWindow.value?.getIsOpen()).toEqual(true)
  })

  // it('infoWindow is close', () => {

  // })
})
